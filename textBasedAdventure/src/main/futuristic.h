#include <iostream>
#include <fstream>

class Futuristic {
public:
  void enter(); // used at the beginning of the game, when the Victorian theme
                // is selected
  void moveTo();   // given choices of where to move at user's current time
  void moveBack(); // Just like moveTo
  void talk();     // talk to the king
  void use();
  void pickUp();


};
